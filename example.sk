bit pointless(int a, int b) {
    if (??) {
      return false;
    }
  else if (??) {
      return a == b;
  }
}

harness void main() {
    int[5] nums = {0, 1, 2, 3, 4};
    bit pointless = pointless(nums[0], nums[1]);
    assert first == 2;
}